{
  "name": "cekirdek-chat-desktop",
  "version": "1.0.0",
  "main": "electron-main.js",
  "scripts": {
    "install:all": "npm install",
    "install:frontend": "[ -d frontend ] && (cd frontend && npm install) || echo \"no frontend\"",
    "install:mobile": "[ -d mobile ] && (cd mobile && npm install) || echo \"no mobile\"",
    "build:frontend": "[ -d frontend ] && (cd frontend && npm run build) || echo \"no frontend to build\"",
    "build": "npm run build:frontend || echo \"no frontend build\"",
    "start": "npm run build && electron .",
    "start:mobile": "cd mobile && npm start",
    "start:signaling": "python signaling/server.py"
  },

  "devDependencies": {
    "electron": "^31.0.0"
  }
}
